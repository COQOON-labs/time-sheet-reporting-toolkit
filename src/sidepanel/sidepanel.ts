/**
 * Sidepanel entry. The actual implementation has been split into focused
 * modules (state, dashboard, report-html, raw-tab, reports-tab,
 * diagnostics-tab) — this file just imports the bootstrap.
 *
 * HTML still references `./sidepanel.ts` so we keep this trampoline.
 */

import './main.js';
